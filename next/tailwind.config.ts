module.exports = {
    darkMode: "class", // Usa a classe "dark" no html root
    theme: {
        extend: {
            colors: {
                background: "hsl(var(--background))",
                foreground: "hsl(var(--foreground))",
                primary: "hsl(var(--primary))",
            },
        },
    },
};
